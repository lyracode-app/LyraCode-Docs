---
title: 简介与概念
---

# 第 1 章：简介与概念——Lyra Code 是什么？

> 这一章写给**第一次接触 Lyra Code** 的你。如果你连「APK」「API Key」「终端」这些词都没听过，完全没关系——我们会从零开始，一个词一个词地解释。
>
> 读这一章预计需要 10 分钟。

---

## 1.1 核心概念：先花 5 分钟搞懂几个词

下面这些词在整份文档里会反复出现。先花几分钟混个脸熟，后面就不会懵了。

### 1.1.1 什么是「AI 模型」？

AI 模型（也叫大语言模型、LLM）可以理解为**一个装在云端服务器里的「超级大脑」**。

- 它平时不装在你手机里，而是在别人的服务器上运行；
- 你的手机把问题发给它，它把答案发回来；
- 常见的模型有：OpenAI 的 GPT 系列、Anthropic 的 Claude 系列、Google 的 Gemini 系列，以及国内的 DeepSeek、Moonshot（Kimi）等。

**关键点**：Lyra Code 本身**不带模型**。你需要自己选择一家模型服务商，把手机和它的服务器连起来，才能开始对话。怎么连，见[第 8 章「模型与多服务商」](/guide/models)。

### 1.1.2 什么是「API Key」？

API Key 是**模型服务商发给你的一串「通行证密码」**，类似：

```
sk-xxxxxxxxxxxxxxxxxxxxxxxx
```

- 它的作用是证明「这台手机是我（付费用户）在用」；
- 你去模型服务商的官网注册账号、充值后，就能在网站上生成自己的 API Key；
- **API Key 相当于钱和身份凭证，千万不能发到网上、不能截图发群里**。泄露了别人就能用你的额度。

### 1.1.3 什么是「Agent（代理）」？

Agent 翻译成「代理」，意思是：**AI 不只是回答你，还能代替你动手做事**。

普通聊天软件：你问 → 它答。

Agent（Lyra Code）：你说「帮我把下载好的压缩包解压，把里面的图片整理到相册文件夹」→ 它会自己规划步骤、读取文件、执行操作，每一步先征求你的同意。

### 1.1.4 什么是「命令 / 终端 / Shell」？

- **命令**：一行行的文字指令，比如 `ls`（列出文件）、`git push`（上传代码）；
- **终端（Terminal）**：输入命令的窗口；
- **Shell**：负责执行命令的「翻译官」程序；
- **Termux**：Android 手机上的一款终端 App，相当于在手机上开了一个「命令行窗口」。

在 Lyra Code 里，AI 可以通过 Termux 帮你执行命令（装软件、跑脚本、管理文件等）。具体配置见[第 2 章「安装与配置」](/guide/install)。

### 1.1.5 什么是「工作区（Workspace）」？

工作区是**一个专门的文件夹**，Lyra Code 的 AI 默认只在这个文件夹里读写文件，防止它乱动你手机上的其他东西。

- 默认工作区：`/storage/emulated/0/Lyra`（也就是手机存储根目录下的 `Lyra` 文件夹）；
- 你可以把项目文件夹、写作草稿等都放在这里。

### 1.1.6 Termux、Shizuku、Root 分别是什么？

| 名字 | 是什么 | 权限级别 | 什么时候需要 |
| --- | --- | --- | --- |
| Termux | 手机上的终端 App | 普通应用权限 | 执行命令、跑脚本、装软件（**几乎人人都需要**） |
| Shizuku | 一个「借用系统调试权限」的服务 | 较高（ADB 级别） | 查询系统信息、批量管理应用等高级操作 |
| Root | 手机被「越狱式」破解后的超级权限 | 最高 | 修改系统文件等极少数场景 |

::: tip 新手提醒
你只需要先搞定 **Termux**。Shizuku 和 Root 用不到就先不管，等以后需要了再回来学（见[第 13 章「命令执行」](/guide/commands)）。
:::

---

## 1.2 Lyra Code 到底是什么？

用一句话说：

> **Lyra Code 是安装在 Android 手机上的一款「本地 AI 助手 + 文件管理器 + 代码编辑器 + 万能工具箱」应用。**

它的特殊之处在于三个词：

### 1.2.1 本地优先（你的数据在你手里）

- 你的模型 Key、对话记录、文件、账号密码，**都保存在手机本地**；
- 不会像某些 App 那样把你的对话上传到它们的服务器再「中转」；
- 你的隐私只在你和模型服务商之间发生。

### 1.2.2 Agent 化（AI 能动手干活，但每次都要你点头）

- AI 可以读你的文件、改你的代码、执行命令、连接远程服务器；
- 但**每一个有风险的动作，都会弹出确认框等你批准**；
- 你不点头，它什么都改不了。这就是「可控」。

### 1.2.3 可扩展（像乐高一样拼功能）

- **Skills**：下载别人的「技能包」，AI 就能学会特定技能（见[第 18 章](/guide/skills)）；
- **MCP**：连接外部工具和数据的标准接口（见[第 15 章](/guide/mcp)）；
- **定时任务**：让 AI 每天早上自动帮你做一件事（见[第 17 章](/guide/scheduled-tasks)）。

---

## 1.3 我适合用吗？

### ✅ 适合你，如果……

- 你想在手机上写代码、改文件、跑脚本；
- 你经常处理邮件、网盘、FTP 服务器，想让 AI 帮你跑腿；
- 你是作者/创作者，想让 AI 帮你写作、整理资料、管理进度；
- 你想体验「AI 自主干活但完全受控」的感觉。

### ❌ 不太适合，如果……

- 你只是想找个聊天机器人闲聊（那用普通 AI 聊天 App 更简单）；
- 你的手机是 Android 8.0 以下的老机型（运行不了）；
- 你完全不想学任何新东西、连「同意权限弹窗」都嫌麻烦。

---

## 1.4 它能帮我做什么？（能力速览）

| 能力 | 通俗解释 | 举个具体例子 | 详见 |
| --- | --- | --- | --- |
| 模型接入 | 接入多家 AI 服务商 | 用 Kimi 聊天、用 Claude 写代码，随时切换 | 第 8 章 |
| 界面与对话 | 四大界面、对话操作与管理 | 建分支、归档会话、图片对话 | 第 3-7 章 |
| Agent 工具 | AI 读写文件、执行命令、联网搜索 | 「帮我看看这个项目里有哪些 TODO」 | 第 11 章 |
| 文件与代码 | 文件管理器 + 代码编辑器 | 解压压缩包、改代码、预览网页 | 第 12-13 章 |
| 远程集成 | 连 SSH / 邮件 / 网盘 / FTP / MCP | 「把服务器上的日志下载下来」 | 第 14-15 章 |
| 微型服务器 | 在手机本地开一个网页服务器 | 预览你自己做的网站 | 第 16 章 |
| 定时任务 | 让 AI 按计划自动工作 | 每天 22:00 生成写作进度总结 | 第 17 章 |
| Skills / 记忆 | 给 AI 装技能、让 AI 记住你的习惯 | 装「小说写作」技能包 | 第 18-19 章 |
| 备份 | 一键备份/恢复所有数据 | 换手机前导出完整备份 | 第 20 章 |
| 统计与诊断 | 看清 Token 花费、检查手机状态 | 对比哪个模型更省钱 | 第 21 章 |

---

## 1.5 常见问题 FAQ

**Q：Lyra Code 收费吗？**
A：应用本身免费开源。你付钱的对象是**模型服务商**（按使用量计费，通常很便宜）。

**Q：一定要有 API Key 才能用吗？**
A：是的。模型跑在服务商服务器上，必须有 Key 才能调用。免费额度很少或没有，建议充少量钱（一般几块钱够用很久）。

**Q：不装 Termux 行不行？**
A：可以，但 AI 会失去执行命令的能力，很多高级功能用不了。建议装，配置见[第 2 章 2.6](/guide/install#_2-6-配置-termux-让-ai-能执行命令)。

**Q：换了新手机怎么把数据搬过去？**
A：用备份功能：旧手机导出备份 → 新手机导入（详见[第 20 章「备份与迁移」](/guide/backup)）。

**Q：手机很卡，是 Lyra Code 的问题吗？**
A：AI 推理在云端，手机本地不跑大模型，一般不会卡。卡顿可能是网络或后台任务。

---

## 1.6 下一步学什么

- [第 2 章：安装与配置](/guide/install) —— 把应用装进手机，打通 Termux 命令通道（必读）
- [快速上手](/guide/quick-start) —— 配置第一个模型服务商，发出第一句问候
- [第 3 章：界面总览](/guide/interface) —— 认识 App 的四大区域

---

*本章完 · 下一章：安装与配置——把应用装进手机、打通命令通道*
